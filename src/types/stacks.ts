export type StackItem = {
    name: string;
    version?: string;
    color: string;
    angle: number;
    distance: number;
    icon: string;
    logoUrl?: string;
    invertLogo?: boolean;
    desc: string;
};